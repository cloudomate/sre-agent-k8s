apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sre-agent-reports
  namespace: sre-agent
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName: "" # leave blank to use cluster default, or specify e.g. "longhorn"
